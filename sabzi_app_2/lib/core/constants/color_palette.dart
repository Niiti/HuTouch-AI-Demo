import 'package:flutter/material.dart';

class ColorPalette {
  static const seedColor = Colors.green;
    static const Color primary = Color(0xFF53B175);
  static const Color primaryVariant = Color(0xFF479D66);
  static const Color secondary = Color(0xFFB0B0B0);
  static const Color background = Colors.white;
  static const Color textPrimary =Color(0xFF181725);
  static const Color textSecondary =  Color(0xFF7C7C7C);
  static const Color divider = Color(0xFFE2E2E2);
  // Define other color constants if needed.
}