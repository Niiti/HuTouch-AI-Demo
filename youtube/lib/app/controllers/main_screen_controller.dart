import 'package:get/get.dart';

class MainScreenController extends GetxController {
  @override
  void onInit() {
    super.onInit();
    try {
      // Initialize any required data here
    } catch (e) {
      print('Error initializing MainScreenController: $e');
    }
  }

  @override
  void onClose() {
    // Dispose resources if needed
    super.onClose();
  }
}
